// Placeholder dev script to keep Turbo happy.
// The Vercel CLI serves serverless functions; this keeps a process alive.
console.log('palette-backend: dev placeholder running. Functions served by `npm --workspace palette_backend run dev:server`.');
setInterval(() => {}, 1 << 30);

